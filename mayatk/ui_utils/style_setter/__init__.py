# !/usr/bin/python
# coding=utf-8
"""Direct import is not necessary or wanted.

Modules are lazy loaded and their exposure is defined in the package's root level main
``__init__``. The engine lives in :mod:`_style_setter`; the shipped color styles live in
``styles/`` (same dir name as blendertk's ``style_setter/styles/``, for parity).
"""
